logger 
{
	file_appender file_debug
	{
		dir = "logs",
		level = info,
		file = "debug",
		type = size,
		max = 10000000,
		suffix = log,
		rotation = 5,
		format = '[%L] (%T) %l%n'
	}

	file_appender file_info
	{
		dir = "logs",
		level = info,
		file = "info",
		type = size,
		max = 10000000,
		suffix = log,
		rotation = 5,
		format = '[%L] (%T) %l%n'
	}

	file_appender file_warn
	{
		dir = "logs",
		level = warn,
		file = "warn",
		type = size,
		max = 10000000,
		suffix = log,
		rotation = 5,
		format = '[%L] (%T) %l%n'		
	}
	
	file_appender file_error
	{
		dir = "logs",
		level = error,
		file = "error",
		type = size,
		max = 10000000,
		suffix = log,
		rotation = 5,
		format = '[%L] (%T) %l%n'		
	}	
	
	console_appender cmd_warn
	{
		level = warn,
		format = '[%L] (%T) %l%n'
	}
	
	console_appender cmd_info
	{
		level = info,
		format = '[%L] (%T) %l%n'
	}	
}
